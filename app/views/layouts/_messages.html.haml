- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{:class => "alert alert-float #{bootstrap_class_for(name)}"}
      %a.close{"data-dismiss" => "alert"} ×
      = content_tag :div, msg, :id => "flash_#{name}"